<div id="carousel_home" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    {% for page in pages %}     
    <li data-target="#carousel_home" data-slide-to="{{ loop.index0 }}" {% if loop.index0 == 0 %} class="active" {% endif %} ></li>    
    {% endfor %} 
  </ol>
    <div id="blured_box" class="blur">
    
    </div>  
  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox" ">
    {% for page in pages %}       
    <div class="item {% if loop.index0 == 0 %} active {% endif %}">      
    {% if page.getPromotedImage() != null %}
    <a href="{{ path('kaikmediapagesmodule_user_display', {'urltitle': page.urltitle}) }}">
     <img class="logo-carousel" src="{{ app.request.scheme ~ '://' ~ app.request.host ~ '/web/uploads/documents/' ~ page.getPromotedImage().getPath()}}" />
    </a>  
     {% endif %}
      <div class="carousel-caption">
       <a href="{{ path('kaikmediapagesmodule_user_display', {'urltitle': page.urltitle}) }}"> <h2 class="text-left"> {{ page.title }} </h2> </a>
        <p class="text-left">
        {{ page.description }}
        </p>
      </div>
    </div>  
    
    
    {% endfor %}    
  </div>
  
</div>
